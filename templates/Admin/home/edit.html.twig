{% extends 'base.html.twig' %}

{% block title %}
    Edition de la Description
{% endblock %}



{% block body %}
    <div class="container mt-4">
        {{ form_start(form) }}
        {{ form_widget(form) }}
        <button type="submit" class="btn btn-success">Enregistrer la description</button>
        {{ form_end(form) }}
    </div>
{% endblock %}

